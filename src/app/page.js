import ProductContent from "./product/ProductContent";

export const metadata = {
  title: "JALJYOTI | Biotech Water Safety",
  description:
    "Jaljyoti's smart paper-based water test: know whether water is safe in about five minutes, at the source, with no lab and no training.",
};

export default function HomePage() {
  return <ProductContent />;
}
